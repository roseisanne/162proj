## instructions to compile code
To compile code I have a gcc (g++) compiler with c++17 support. To run this program, run "g++ main.cpp pipeline.cpp -o main" in the project directory

## Parameters and configurations
For this code I am using the version C++ 17 and what terminal to run my code I use Ubuntu (WSL), but you can also use powershell and git terminals as long as you have the type of compiler. To run this code first compile using the command from above and then run "./main" to get the results of the program.